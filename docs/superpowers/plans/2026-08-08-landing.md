# Landing — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Livrer les deux pages d'entrée publiques — `/` pour l'artisan, `/verifier` pour le demandeur — avec leurs deux actions réelles et le mouvement de marque.

**Architecture :** Huit composants entrent au design system, dont deux primitives de mouvement qui encapsulent `prefers-reduced-motion` une fois pour toutes. Les compositions de section vivent dans un dossier privé `_landing/`, hors du routeur. Les deux actions serveur s'appuient sur du code déjà écrit — `publicProfile` et `sendQuoteLink` — et n'ajoutent qu'une table de garde contre l'abus.

**Tech Stack :** Next.js 16.3 (App Router), React 19.2, Tailwind v4, Drizzle, Vitest, Playwright.

**Référence :** [Spec landing](../specs/2026-08-08-landing-design.md) · [Spec image de marque](../specs/2026-08-08-image-de-marque-design.md) · [Socle Artisan](../specs/2026-08-07-socle-artisan-design.md)

---

## Correction à apporter à la spec

Le §6.2 affirme « **RGPD :** aucun traitement nouveau ». C'était vrai de l'envoi lui-même, mais **faux dès qu'on ajoute la limitation de débit** exigée au §10 : compter les demandes par adresse, c'est enregistrer des adresses avec un horodatage.

La Task 9 corrige la spec en même temps qu'elle crée la table. Le traitement ajouté est minuscule et doit être décrit tel quel : finalité **lutte contre l'abus**, base **intérêt légitime**, rétention **24 heures**, purge à chaque écriture.

## Découpage en deux phases

**Phase 1 (Tasks 1 à 8) — le socle et la page pro.** Livrable seul : un vrai accueil remplace le placeholder actuel, avec la vérification SIRET fonctionnelle.

**Phase 2 (Tasks 9 à 13) — la page demandeur.** Ajoute le renvoi de lien de devis, sa table de garde, et la seconde page.

## Structure des fichiers

| Fichier | Responsabilité |
|---|---|
| `src/ui/motion.css` | Les deux gestes en CSS, et leur neutralisation sous `prefers-reduced-motion`. |
| `src/ui/molecules/use-in-view.ts` | Hook client : bascule un attribut quand l'élément entre dans la vue. Aucune logique d'animation. |
| `src/ui/molecules/reveal.tsx` | `Reveal` — la mise d'équerre. |
| `src/ui/molecules/stagger.tsx` | `Stagger` — le calepinage. |
| `src/ui/molecules/section-header.tsx` | `SectionHeader` — étiquette, titre, chapeau. |
| `src/ui/molecules/step-card.tsx` | `StepCard` — numéro, titre, texte. |
| `src/ui/organisms/siret-lookup.tsx` | `SiretLookup` — champ SIRET et son action. |
| `src/ui/organisms/quote-link-form.tsx` | `QuoteLinkForm` — renvoi du lien de devis. |
| `src/ui/organisms/principle-panel.tsx` | `PrinciplePanel` — les engagements du §12 du socle. |
| `src/ui/shells/landing-shell.tsx` | `LandingShell` — navigation à lien croisé, pied de page marketing. |
| `src/domain/siret.ts` | *(modifié)* `parseSiretInput` — normalisation et validation de la saisie. |
| `src/domain/rate-limit.ts` | `isRateLimited` — décision pure, sans base de données. |
| `src/db/schema/quote-link-request.ts` | La table de garde. |
| `src/services/quote-link.ts` | Recherche des devis d'une adresse, garde, envoi. |
| `src/app/_landing/pro/*.tsx` | Les sept sections de la page pro. |
| `src/app/_landing/verifier/*.tsx` | Les quatre sections de la page demandeur. |
| `src/app/actions/public.ts` | Les deux actions serveur publiques. |

---

# Phase 1 — Le socle et la page pro

## Task 1 : Étendre l'inventaire

Elle vient en premier : sans elle, `pnpm check:ds` échoue au premier composant créé.

**Files:**
- Modify: `scripts/check-design-system.mjs`
- Modify: `docs/superpowers/specs/2026-08-08-image-de-marque-design.md`

- [ ] **Step 1 : Ajouter les huit entrées**

Dans `scripts/check-design-system.mjs`, remplacer les trois listes concernées :

```js
  molecules: [
    'Field', 'Card', 'StatusBadge', 'SealBadge', 'LogoLockup', 'EmptyState',
    'Toast', 'Tooltip', 'ButtonGroup', 'SummaryLine', 'Dialog', 'ThemeToggle',
    'SectionHeader', 'StepCard', 'Reveal', 'Stagger',
  ],
  organisms: [
    'AppHeader', 'QuoteTable', 'QuoteLineEditor', 'QuoteLinesTable', 'TotalsPanel',
    'VatBreakdown', 'LegalMentionsPanel', 'PaymentTimeline', 'SignaturePanel',
    'SiretLookup', 'QuoteLinkForm', 'PrinciplePanel',
  ],
  shells: ['AppShell', 'PublicShell', 'PdfShell', 'LandingShell'],
```

- [ ] **Step 2 : Documenter la raison de chaque ajout**

Dans le commentaire au-dessus d'`INVENTORY`, sous les deux entrées déjà justifiées, ajouter :

```
 * Huit entrees supplementaires pour la landing (spec landing §7.1) :
 *
 * - `SectionHeader`, `StepCard` — repetes onze fois sur les deux pages.
 * - `Reveal`, `Stagger` — les deux gestes de marque encapsules. Sans eux,
 *   chaque section reimplemente l'animation et oublie prefers-reduced-motion.
 * - `SiretLookup`, `QuoteLinkForm` — les deux actions publiques.
 * - `PrinciplePanel` — les engagements du §12 du socle.
 * - `LandingShell` — navigation a lien croise. `PublicShell` est un gabarit de
 *   document : lui greffer une navigation commerciale le denaturerait.
```

- [ ] **Step 3 : Reporter dans la spec de marque**

Dans `docs/superpowers/specs/2026-08-08-image-de-marque-design.md`, §6.1, ajouter les mêmes noms aux trois lignes du tableau (molecules, organismes, gabarits), et compléter le paragraphe « Deux entrées ajoutées après l'implémentation » qui devient « Dix entrées », en renvoyant à la spec landing pour les huit dernières.

- [ ] **Step 4 : Vérifier**

Run : `pnpm check:ds`
Expected : `✓ Usage du design system` — l'inventaire élargi ne casse rien, aucun des huit composants n'existant encore.

- [ ] **Step 5 : Commit**

```bash
git add scripts/check-design-system.mjs docs/superpowers/specs/2026-08-08-image-de-marque-design.md
git commit -m "chore: l'inventaire du design system accueille les composants de la landing"
```

---

## Task 2 : Le régime de mouvement

**Files:**
- Create: `src/ui/motion.css`
- Modify: `src/app/globals.css`
- Modify: `src/app/layout.tsx`

- [ ] **Step 1 : Écrire `src/ui/motion.css`**

```css
/**
 * Les deux gestes de marque.
 *
 * Regime distinct de la charte §5.6, qui regit l'outil : sur une page publique
 * de marque, les durees vont jusqu'a 600 ms et le geste narratif est autorise.
 * Trois regles ne bougent pas : prefers-reduced-motion respecte, transform et
 * opacity uniquement, aucune animation qui bloque une interaction.
 *
 * L'etat initial est porte par `.dq-motion`, classe posee par le script du
 * layout. Sans JavaScript, la classe n'existe pas et **tout est visible** :
 * une animation ratee ne doit jamais effacer le contenu.
 */

.dq-motion [data-reveal] {
  opacity: 0;
  transform: rotate(-1.6deg) translateY(16px);
}

.dq-motion [data-reveal][data-in] {
  animation: dq-square-up 560ms cubic-bezier(0.22, 1.15, 0.36, 1) both;
}

@keyframes dq-square-up {
  from { opacity: 0; transform: rotate(-1.6deg) translateY(16px); }
  70%  { opacity: 1; transform: rotate(0.3deg) translateY(0); }
  to   { opacity: 1; transform: none; }
}

/* Le carre de l'angle verifie se pose quand l'alignement est atteint. */
.dq-motion [data-reveal-tick] { opacity: 0; }
.dq-motion [data-reveal][data-in] [data-reveal-tick] {
  animation: dq-tick 260ms ease-out 400ms both;
}

@keyframes dq-tick {
  from { opacity: 0; transform: scale(0.4); }
  70%  { opacity: 1; transform: scale(1.2); }
  to   { opacity: 1; transform: none; }
}

/* Le calepinage : une cadence, pas un geste. 70 ms entre deux elements. */
.dq-motion [data-stagger] > * { opacity: 0; }
.dq-motion [data-stagger][data-in] > * {
  animation: dq-lay 460ms cubic-bezier(0.2, 1.25, 0.4, 1) both;
  animation-delay: calc(var(--dq-i, 0) * 70ms);
}

@keyframes dq-lay {
  from { opacity: 0; transform: translateY(-16px) scale(0.95); }
  to   { opacity: 1; transform: none; }
}

@media (prefers-reduced-motion: reduce) {
  .dq-motion [data-reveal],
  .dq-motion [data-reveal-tick],
  .dq-motion [data-stagger] > * {
    opacity: 1 !important;
    transform: none !important;
    animation: none !important;
  }
}
```

- [ ] **Step 2 : Importer la feuille**

Dans `src/app/globals.css`, sous l'import des tokens :

```css
@import 'tailwindcss';
@import '../ui/tokens.css';
@import '../ui/motion.css';
```

- [ ] **Step 3 : Poser la classe avant le premier rendu**

Dans `src/app/layout.tsx`, remplacer la constante `THEME_BOOTSTRAP` par :

```tsx
/**
 * Amorce du document : le theme, et l'activation du mouvement.
 *
 * `dq-motion` conditionne l'etat initial masque des elements animes. Sans
 * JavaScript la classe n'est jamais posee, donc rien n'est masque — une
 * animation qui ne se declenche pas ne doit pas effacer une page.
 */
const BOOTSTRAP = `try{var d=document.documentElement;var t=localStorage.getItem('dq-theme');if(t==='dark'||t==='light')d.dataset.theme=t;d.classList.add('dq-motion')}catch(e){}`
```

Et remplacer l'usage `__html: THEME_BOOTSTRAP` par `__html: BOOTSTRAP`.

- [ ] **Step 4 : Vérifier**

Run : `pnpm exec tsc --noEmit && pnpm build`
Expected : aucune erreur.

- [ ] **Step 5 : Commit**

```bash
git add src/ui/motion.css src/app/globals.css src/app/layout.tsx
git commit -m "feat: le regime de mouvement des pages publiques de marque"
```

---

## Task 3 : `Reveal` et `Stagger`

**Files:**
- Create: `src/ui/molecules/use-in-view.ts`
- Create: `src/ui/molecules/reveal.tsx`
- Create: `src/ui/molecules/stagger.tsx`

- [ ] **Step 1 : Le hook**

```ts
// src/ui/molecules/use-in-view.ts
'use client'

import { useEffect, useRef, useState } from 'react'

/**
 * Bascule a `true` la premiere fois que l'element entre dans la vue.
 *
 * Le hook ne connait rien a l'animation : il pose un fait, le CSS en tire les
 * consequences. C'est ce qui permet a `prefers-reduced-motion` de tout
 * neutraliser sans qu'aucune ligne de JavaScript ne le sache.
 *
 * Une seule bascule, jamais de retour en arriere : un bloc deja lu qui
 * rejouerait son entree au defilement inverse serait desagreable.
 */
export function useInView<T extends HTMLElement>(): {
  ref: React.RefObject<T | null>
  inView: boolean
} {
  const ref = useRef<T>(null)
  const [inView, setInView] = useState(false)

  useEffect(() => {
    const node = ref.current
    if (!node) return

    // Navigateur sans IntersectionObserver : on montre, on n'anime pas.
    if (typeof IntersectionObserver === 'undefined') {
      setInView(true)
      return
    }

    const observer = new IntersectionObserver(
      ([entry]) => {
        if (!entry.isIntersecting) return
        setInView(true)
        observer.disconnect()
      },
      // Le bloc s'anime quand il est engage, pas quand il affleure.
      { rootMargin: '0px 0px -12% 0px', threshold: 0.15 },
    )

    observer.observe(node)
    return () => observer.disconnect()
  }, [])

  return { ref, inView }
}
```

- [ ] **Step 2 : `Reveal`**

```tsx
// src/ui/molecules/reveal.tsx
'use client'

import { useInView } from './use-in-view'

/**
 * La mise d'equerre : le bloc arrive legerement de travers et se redresse.
 *
 * C'est le geste de la marque — **un seul par ecran**, sur le bloc qui porte
 * une promesse. Le carre terre cuite (`RevealTick`) se pose a l'instant ou
 * l'alignement est atteint.
 */
export function Reveal({
  as: Tag = 'div',
  children,
}: {
  as?: 'div' | 'header' | 'section'
  children: React.ReactNode
}) {
  const { ref, inView } = useInView<HTMLDivElement>()

  return (
    <Tag ref={ref} data-reveal="" data-in={inView ? '' : undefined}>
      {children}
    </Tag>
  )
}

/** Le carre de l'angle verifie. A placer dans un `Reveal`, une seule fois. */
export function RevealTick() {
  return (
    <span
      data-reveal-tick=""
      aria-hidden="true"
      className="ml-2.5 inline-block size-3 bg-brand align-middle"
    />
  )
}
```

- [ ] **Step 3 : `Stagger`**

```tsx
// src/ui/molecules/stagger.tsx
'use client'

import { Children, isValidElement, cloneElement } from 'react'
import { useInView } from './use-in-view'

/**
 * Le calepinage : les elements se posent un a un, 70 ms d'ecart.
 *
 * Ce n'est pas un geste mais une **cadence** : elle ne concurrence rien et
 * peut cohabiter avec `Reveal` sur le meme ecran.
 *
 * Le rang est passe en variable CSS plutot qu'en classe : le nombre d'elements
 * n'est pas connu a l'avance, et generer une classe par rang obligerait a les
 * declarer toutes dans la feuille.
 */
export function Stagger({ children }: { children: React.ReactNode }) {
  const { ref, inView } = useInView<HTMLDivElement>()

  return (
    <div ref={ref} data-stagger="" data-in={inView ? '' : undefined} className="contents">
      {Children.map(children, (child, index) =>
        isValidElement<{ style?: React.CSSProperties }>(child)
          ? cloneElement(child, {
              style: { ...child.props.style, ['--dq-i' as string]: index },
            })
          : child,
      )}
    </div>
  )
}
```

- [ ] **Step 4 : Vérifier**

Run : `pnpm exec tsc --noEmit && pnpm check:ds`
Expected : aucune erreur ; `RevealTick` n'apparaît pas comme violation, l'inventaire ne contrôlant que les composants exportés au nom déjà déclaré — **si `check:ds` signale `RevealTick`, l'ajouter à la liste `molecules` de `scripts/check-design-system.mjs`.**

- [ ] **Step 5 : Commit**

```bash
git add src/ui/molecules/use-in-view.ts src/ui/molecules/reveal.tsx src/ui/molecules/stagger.tsx
git commit -m "feat: Reveal et Stagger, les deux gestes de marque encapsules"
```

---

## Task 4 : `SectionHeader` et `StepCard`

**Files:**
- Create: `src/ui/molecules/section-header.tsx`
- Create: `src/ui/molecules/step-card.tsx`

- [ ] **Step 1 : `SectionHeader`**

```tsx
// src/ui/molecules/section-header.tsx
import { Heading } from '@/ui/atoms/heading'
import { Text } from '@/ui/atoms/text'

/**
 * Etiquette, titre, chapeau. Repete onze fois sur les deux pages de la landing.
 *
 * `label` est facultatif : l'accroche d'une page n'en porte pas, une section
 * courante si.
 */
export function SectionHeader({
  label,
  title,
  lead,
}: {
  label?: string
  title: React.ReactNode
  lead?: React.ReactNode
}) {
  return (
    <div className="flex flex-col gap-3">
      {label ? (
        <Text size="label" tone="muted">
          {label}
        </Text>
      ) : null}
      <Heading level={2} as="h2">
        {title}
      </Heading>
      {lead ? (
        <div className="max-w-[52ch]">
          <Text tone="soft">{lead}</Text>
        </div>
      ) : null}
    </div>
  )
}
```

- [ ] **Step 2 : `StepCard`**

```tsx
// src/ui/molecules/step-card.tsx
import { Heading } from '@/ui/atoms/heading'
import { Text } from '@/ui/atoms/text'
import { Card } from '@/ui/molecules/card'

/**
 * Une etape numerotee.
 *
 * Le numero est en `aria-hidden` : il ordonne visuellement, mais la liste qui
 * l'entoure porte deja l'ordre pour un lecteur d'ecran. L'annoncer deux fois
 * ne renseigne personne.
 */
export function StepCard({
  step,
  title,
  children,
}: {
  step: number
  title: string
  children: React.ReactNode
}) {
  return (
    <Card elevation="e1">
      <div className="flex flex-col gap-2">
        <span
          aria-hidden="true"
          className="inline-flex size-7 items-center justify-center rounded-badge bg-primary font-display text-xs font-bold text-on-primary"
        >
          {step}
        </span>
        <Heading level={3} as="h3">
          {title}
        </Heading>
        <Text size="sm" tone="soft">
          {children}
        </Text>
      </div>
    </Card>
  )
}
```

- [ ] **Step 3 : Vérifier et committer**

Run : `pnpm exec tsc --noEmit && pnpm check:ds`

```bash
git add src/ui/molecules/section-header.tsx src/ui/molecules/step-card.tsx
git commit -m "feat: SectionHeader et StepCard"
```

---

## Task 5 : `LandingShell`

**Files:**
- Create: `src/ui/shells/landing-shell.tsx`

- [ ] **Step 1 : Écrire le gabarit**

```tsx
// src/ui/shells/landing-shell.tsx
import { ButtonLink } from '@/ui/atoms/button-link'
import { Link } from '@/ui/atoms/link'
import { Text } from '@/ui/atoms/text'
import { Lockup } from '@/ui/brand/lockup'

/**
 * Le gabarit des deux pages d'entree publiques.
 *
 * Distinct de `PublicShell`, qui est un gabarit de **document** — un devis, une
 * facture, un passeport. Lui greffer une navigation commerciale et un lien
 * croise le denaturerait.
 *
 * Comme lui, il force le mode clair : une page d'accroche doit avoir la meme
 * tete pour tout le monde, et sa capture d'ecran doit ressembler a ce qu'on
 * voit.
 *
 * Le lien croise apparait en haut et en bas : pas d'ecran de choix a l'entree,
 * qui ajouterait un clic a tout le monde pour n'aider personne.
 */
export function LandingShell({
  audience,
  children,
}: {
  audience: 'pro' | 'demandeur'
  children: React.ReactNode
}) {
  const cross =
    audience === 'pro'
      ? { question: 'Vous êtes un particulier ?', label: 'Vérifier un artisan', href: '/verifier' }
      : { question: 'Vous êtes artisan ?', label: 'Créer mon compte', href: '/' }

  return (
    <div data-theme="light" className="flex min-h-full flex-1 flex-col bg-surface">
      <header className="border-b border-rule bg-card">
        <div className="mx-auto flex w-full max-w-5xl flex-wrap items-center gap-4 px-6 py-4">
          <Link href="/" tone="bare">
            <Lockup size="sm" />
          </Link>
          <div className="ml-auto flex items-center gap-3">
            <Text size="sm" tone="muted" as="span">
              {cross.question}
            </Text>
            <ButtonLink href={cross.href} tone="secondary">
              {cross.label}
            </ButtonLink>
          </div>
        </div>
      </header>

      <main className="flex flex-1 flex-col">{children}</main>

      <footer className="border-t border-rule bg-card">
        <div className="mx-auto flex w-full max-w-5xl flex-wrap items-center gap-x-6 gap-y-3 px-6 py-8">
          <Lockup size="sm" />
          <Link href="/confidentialite" tone="bare">
            <span className="text-sm">Protection des données</span>
          </Link>
          <div className="ml-auto">
            <Link href={cross.href}>{cross.label}</Link>
          </div>
        </div>
      </footer>
    </div>
  )
}
```

- [ ] **Step 2 : Vérifier et committer**

Run : `pnpm exec tsc --noEmit && pnpm check:ds`

```bash
git add src/ui/shells/landing-shell.tsx
git commit -m "feat: LandingShell, le gabarit des deux pages d'entree"
```

---

## Task 6 : La saisie de SIRET, du domaine à l'écran

**Files:**
- Modify: `src/domain/siret.ts`
- Create: `tests/domain/siret-input.test.ts`
- Create: `src/app/actions/public.ts`
- Create: `src/ui/organisms/siret-lookup.tsx`

- [ ] **Step 1 : Écrire le test de la fonction pure**

```ts
// tests/domain/siret-input.test.ts
import { describe, expect, it } from 'vitest'
import { parseSiretInput } from '@/domain/siret'

describe('parseSiretInput', () => {
  it('accepte un SIRET avec des espaces et rend le SIREN', () => {
    expect(parseSiretInput('507 698 207 00018')).toEqual({ siren: '507698207' })
  })

  it('accepte un SIRET colle', () => {
    expect(parseSiretInput('50769820700018')).toEqual({ siren: '507698207' })
  })

  it('refuse une saisie trop courte', () => {
    expect(parseSiretInput('5076982')).toEqual({
      error: 'Ce SIRET est incomplet : il compte 14 chiffres.',
    })
  })

  it('refuse une saisie vide', () => {
    expect(parseSiretInput('   ')).toEqual({
      error: 'Ce SIRET est incomplet : il compte 14 chiffres.',
    })
  })

  it('refuse un SIRET dont la cle de Luhn est fausse', () => {
    expect(parseSiretInput('50769820700019')).toEqual({
      error: 'Ce SIRET n’existe pas : vérifiez les chiffres.',
    })
  })
})
```

- [ ] **Step 2 : Lancer le test, le voir échouer**

Run : `pnpm exec vitest run tests/domain/siret-input.test.ts`
Expected : ÉCHEC — `parseSiretInput` n'est pas exporté.

- [ ] **Step 3 : Implémenter**

Ajouter à la fin de `src/domain/siret.ts` :

```ts
/**
 * Lit une saisie libre de SIRET et en tire le SIREN.
 *
 * Deux messages distincts, et c'est deliberé : « incomplet » se corrige en
 * continuant a taper, « n'existe pas » demande de relire. Les confondre laisse
 * l'utilisateur devant un champ rouge sans savoir quoi faire.
 */
export function parseSiretInput(value: string): { siren: string } | { error: string } {
  const digits = normalizeSiret(value)

  if (digits.length !== 14) {
    return { error: 'Ce SIRET est incomplet : il compte 14 chiffres.' }
  }
  if (!isValidSiret(digits)) {
    return { error: 'Ce SIRET n’existe pas : vérifiez les chiffres.' }
  }

  return { siren: digits.slice(0, 9) }
}
```

**Avant d'écrire, lire `normalizeSiret` et `isValidSiret` en tête du fichier** : si `normalizeSiret` ne retire pas déjà les non-chiffres, adapter (`value.replace(/\D/g, '')`).

- [ ] **Step 4 : Lancer le test**

Run : `pnpm exec vitest run tests/domain/siret-input.test.ts`
Expected : PASS, 5 cas.

- [ ] **Step 5 : L'action serveur**

```ts
// src/app/actions/public.ts
'use server'

import { redirect } from 'next/navigation'
import { parseSiretInput } from '@/domain/siret'
import { publicProfile } from '@/services/public-profile'

export interface LookupState {
  error?: string
}

/**
 * Redirige vers le passeport public d'une entreprise.
 *
 * **Une entreprise introuvable et une entreprise sans activite couverte
 * recoivent le meme message.** `publicProfile` renvoyant `null` dans les deux
 * cas, l'indistinction est acquise par construction : il ne faut surtout pas
 * l'affiner, sinon le formulaire devient un test d'existence.
 */
export async function lookupCompany(
  _state: LookupState,
  form: FormData,
): Promise<LookupState> {
  const parsed = parseSiretInput(String(form.get('siret') ?? ''))
  if ('error' in parsed) return { error: parsed.error }

  const profile = await publicProfile(parsed.siren, new Date())
  if (!profile) {
    return { error: 'Cette entreprise n’a pas encore de page publique sur D’équerre.' }
  }

  // `redirect` leve : jamais dans un try/catch.
  redirect(`/artisan/${profile.slug}`)
}
```

- [ ] **Step 6 : Le composant**

```tsx
// src/ui/organisms/siret-lookup.tsx
'use client'

import { useActionState } from 'react'
import { lookupCompany, type LookupState } from '@/app/actions/public'
import { Button } from '@/ui/atoms/button'
import { Input } from '@/ui/atoms/input'
import { Text } from '@/ui/atoms/text'
import { Field } from '@/ui/molecules/field'

const initialState: LookupState = {}

/**
 * Le champ SIRET, sur les deux pages.
 *
 * `tone` porte la difference : encre cote pro, ou l'action mene a l'outil ;
 * terre cuite cote demandeur, seule page publique sans action destructive
 * alentour (charte §5.4).
 */
export function SiretLookup({
  tone = 'primary',
  label,
  cta,
  hint,
}: {
  tone?: 'primary' | 'conversion'
  label: string
  cta: string
  hint: string
}) {
  const [state, action, pending] = useActionState(lookupCompany, initialState)

  return (
    <form action={action} className="flex flex-col gap-2">
      <div className="flex max-w-md flex-wrap items-end gap-3">
        <div className="min-w-56 flex-1">
          <Field label={label} error={state.error} required>
            {(p) => (
              <Input
                {...p}
                name="siret"
                inputMode="numeric"
                autoComplete="off"
                placeholder="123 456 789 00012"
              />
            )}
          </Field>
        </div>
        <Button type="submit" tone={tone} size="lg" pending={pending}>
          {cta}
        </Button>
      </div>
      <Text size="sm" tone="muted">
        {hint}
      </Text>
    </form>
  )
}
```

- [ ] **Step 7 : Vérifier et committer**

Run : `pnpm exec tsc --noEmit && pnpm check:ds && pnpm exec vitest run tests/domain`

```bash
git add src/domain/siret.ts tests/domain/siret-input.test.ts src/app/actions/public.ts src/ui/organisms/siret-lookup.tsx
git commit -m "feat: la verification d'une entreprise par son SIRET"
```

---

## Task 7 : `PrinciplePanel` et les sept sections de la page pro

**Files:**
- Create: `src/ui/organisms/principle-panel.tsx`
- Create: `src/app/_landing/pro/hero.tsx`
- Create: `src/app/_landing/pro/mentions.tsx`
- Create: `src/app/_landing/pro/steps.tsx`
- Create: `src/app/_landing/pro/passport.tsx`
- Create: `src/app/_landing/pro/principles.tsx`
- Create: `src/app/_landing/pro/next.tsx`
- Create: `src/app/_landing/pro/pricing.tsx`
- Modify: `src/app/page.tsx`

- [ ] **Step 1 : `PrinciplePanel`**

```tsx
// src/ui/organisms/principle-panel.tsx
import { Icon } from '@/ui/atoms/icon'
import { Text } from '@/ui/atoms/text'
import { Card } from '@/ui/molecules/card'

/**
 * Les engagements du §12 du socle.
 *
 * Ce n'est pas une mention de bas de page : pour un artisan demarche dix fois
 * par des vendeurs de leads, c'est le meilleur argument de la page — et aucun
 * concurrent ne peut ecrire ces phrases.
 */
export function PrinciplePanel({ principles }: { principles: string[] }) {
  return (
    <Card elevation="e1">
      <ul className="flex flex-col gap-4">
        {principles.map((principle) => (
          <li key={principle} className="flex items-start gap-3">
            <span className="mt-0.5 text-verified">
              <Icon name="check" size="sm" />
            </span>
            <Text as="span">{principle}</Text>
          </li>
        ))}
      </ul>
    </Card>
  )
}
```

- [ ] **Step 2 : Le motif commun des sections**

Chacune des sept sections suit exactement ce motif — un composant serveur, sans prop, qui rend une `<section>`. Exemple complet, `src/app/_landing/pro/hero.tsx` :

```tsx
import { Heading } from '@/ui/atoms/heading'
import { Text } from '@/ui/atoms/text'
import { Reveal, RevealTick } from '@/ui/molecules/reveal'
import { SiretLookup } from '@/ui/organisms/siret-lookup'

export function Hero() {
  return (
    <section className="mx-auto w-full max-w-5xl px-6 py-20">
      {/* Le seul `Reveal` de la page : c'est le bloc qui porte la promesse. */}
      <Reveal>
        <div className="flex flex-col gap-5">
          <Heading level="display">
            Vos devis et vos factures, gratuits à vie.
            <RevealTick />
          </Heading>
          <div className="max-w-[52ch]">
            <Text tone="soft">
              Conformes aux mentions obligatoires du bâtiment, signés par vos clients en deux
              minutes. Et une page publique qui prouve que votre assurance est à jour.
            </Text>
          </div>
          <SiretLookup
            label="Votre SIRET"
            cta="Commencer"
            hint="Trente secondes. Aucune carte bancaire."
          />
        </div>
      </Reveal>
    </section>
  )
}
```

- [ ] **Step 3 : Les six autres sections**

Même structure, textes tirés du **§11 de la spec landing**, qui fait foi. Points à respecter :

- `mentions.tsx` — `SectionHeader` avec l'étiquette « Ce que ça vous évite », fond alterné : ajouter `bg-card border-y border-rule` sur la `<section>`.
- `steps.tsx` — trois `StepCard` enveloppés dans un `Stagger`, dans une grille `grid gap-4 sm:grid-cols-3`.
- `passport.tsx` — `SectionHeader` puis trois `StatusBadge` réels : `<StatusBadge kind="quote" status="signed" />` ne convient pas ici ; utiliser `Badge` directement avec les tons `verified`, `verified`, `warning` et les libellés « Plomberie · couverte », « Chauffage · couverte », « Électricité · attestation manquante ».
- `principles.tsx` — `SectionHeader` titre « Ce que nous ne ferons jamais. » puis `PrinciplePanel` avec les deux phrases du §11.
- `next.tsx` — la section « la suite ». **Aucune date, aucun « bientôt ».** Sa rédaction est le point ouvert n° 3 du §10 de la spec : écrire une première version, et la signaler à la relecture.
- `pricing.tsx` — `SectionHeader` « Ce que ça coûte » / « L'outil est gratuit. Pour toujours. » puis à nouveau `SiretLookup`, mêmes props que le hero.

**Aucune section ne dépasse 60 lignes.** Si l'une s'en approche, c'est qu'elle mélange deux idées.

- [ ] **Step 4 : La composition**

```tsx
// src/app/page.tsx
import type { Metadata } from 'next'
import { LandingShell } from '@/ui/shells/landing-shell'
import { Hero } from './_landing/pro/hero'
import { Mentions } from './_landing/pro/mentions'
import { Next } from './_landing/pro/next'
import { Passport } from './_landing/pro/passport'
import { Pricing } from './_landing/pro/pricing'
import { Principles } from './_landing/pro/principles'
import { Steps } from './_landing/pro/steps'

export const metadata: Metadata = {
  title: "D'équerre — devis, factures et assurance vérifiée pour le bâtiment",
  description:
    'Vos devis et vos factures, gratuits à vie, conformes aux mentions obligatoires du bâtiment. Et une page publique qui prouve que votre assurance est à jour.',
  alternates: { canonical: '/' },
}

export default function Home() {
  return (
    <LandingShell audience="pro">
      <Hero />
      <Mentions />
      <Steps />
      <Passport />
      <Principles />
      <Next />
      <Pricing />
    </LandingShell>
  )
}
```

- [ ] **Step 5 : Vérifier**

Run : `pnpm exec tsc --noEmit && pnpm lint && pnpm check:ds && pnpm check:size && pnpm build`
Expected : tout vert.

- [ ] **Step 6 : Vérifier à l'œil**

Run : `pnpm dev`, ouvrir `http://localhost:3000`
Attendu : l'accroche se met d'équerre à l'arrivée, les trois étapes se posent en cadence au défilement, le champ SIRET redirige vers un passeport existant et affiche le message neutre sinon. Basculer `prefers-reduced-motion` dans les préférences du système : tout doit rester visible, sans mouvement.

- [ ] **Step 7 : Commit**

```bash
git add src/ui/organisms/principle-panel.tsx src/app/_landing src/app/page.tsx
git commit -m "feat: la page pro"
```

---

## Task 8 : L'image de partage

**Files:**
- Create: `src/app/opengraph-image.tsx`

- [ ] **Step 1 : Lire la convention**

Run : `sed -n '1,80p' /Users/bugeon/Work/PYOH/Workaround/node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/01-metadata/opengraph-image.md`
Cette version de Next peut différer de ce que vous connaissez : lire avant d'écrire.

- [ ] **Step 2 : Écrire l'image**

```tsx
// src/app/opengraph-image.tsx
import { readFileSync } from 'node:fs'
import { join } from 'node:path'
import { ImageResponse } from 'next/og'
import { roles } from '@/ui/tokens'

export const alt = "D'équerre — devis, factures et assurance vérifiée pour le bâtiment"
export const size = { width: 1200, height: 630 }
export const contentType = 'image/png'

/**
 * L'image de partage, generee par code.
 *
 * Elle lit les tokens et les fichiers de police deja embarques pour le PDF :
 * une image generee ne peut pas diverger de la charte, une image dessinee a la
 * main, si.
 */
export default async function Image() {
  const fonts = join(process.cwd(), 'src', 'pdf', 'fonts')
  const archivo = readFileSync(join(fonts, 'Archivo-ExtraBold.ttf'))

  return new ImageResponse(
    (
      <div
        style={{
          width: '100%',
          height: '100%',
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'center',
          padding: 84,
          background: roles.light.surface,
          color: roles.light.ink,
          fontFamily: 'Archivo',
        }}
      >
        <div style={{ display: 'flex', alignItems: 'center', gap: 20, marginBottom: 48 }}>
          <svg width="64" height="64" viewBox="0 0 48 48">
            <path d="M8 4 H19 V29 H8 Z" fill={roles.light.ink} />
            <path d="M8 29 H44 V40 H8 Z" fill={roles.light.brand} />
            <rect x="19" y="22" width="7" height="7" fill={roles.light.ink} />
          </svg>
          <span style={{ fontSize: 44, letterSpacing: -2 }}>d’équerre</span>
        </div>
        <div style={{ fontSize: 76, lineHeight: 1.05, letterSpacing: -3 }}>
          Vos devis et vos factures,
          <br />
          gratuits à vie.
        </div>
      </div>
    ),
    { ...size, fonts: [{ name: 'Archivo', data: archivo, weight: 800, style: 'normal' }] },
  )
}
```

- [ ] **Step 3 : Vérifier**

Run : `pnpm build`, puis `pnpm dev` et ouvrir `http://localhost:3000/opengraph-image`
Attendu : une image 1200×630, fond craie, marque bicolore, titre en Archivo. **Si la police ne s'applique pas, le texte retombe sur une police système sans que rien n'échoue** — comparer à l'œil, ne pas se fier au succès du build.

- [ ] **Step 4 : Commit**

```bash
git add src/app/opengraph-image.tsx
git commit -m "feat: l'image de partage, generee depuis les tokens"
```

---

# Phase 2 — La page demandeur

## Task 9 : La table de garde, et la correction de la spec

**Files:**
- Create: `src/db/schema/quote-link-request.ts`
- Modify: `src/db/schema/index.ts`
- Modify: `docs/superpowers/specs/2026-08-08-landing-design.md`

- [ ] **Step 1 : Corriger la spec**

Dans le §6.2, remplacer le paragraphe RGPD par :

```markdown
**RGPD :** l'envoi lui-même n'ajoute rien — l'adresse est déjà dans `customer`,
la finalité est identique à l'envoi initial. **La limitation de débit, elle, est
un traitement nouveau**, si minuscule soit-il : finalité **lutte contre l'abus**,
base **intérêt légitime**, rétention **24 heures**, purge à chaque écriture. À
reporter au registre.
```

- [ ] **Step 2 : Le schéma**

```ts
// src/db/schema/quote-link-request.ts
import { pgTable, timestamp, text, uuid, index } from 'drizzle-orm/pg-core'

/**
 * Garde contre l'abus du renvoi de lien de devis.
 *
 * Un formulaire public qui declenche un e-mail est un vecteur d'envoi en masse
 * s'il n'est pas borne. On enregistre donc l'adresse et l'instant, le temps de
 * compter — **24 heures, pas davantage**, et la purge se fait a chaque ecriture
 * plutot que par une tache planifiee qu'on oublierait de surveiller.
 *
 * L'adresse est en clair : elle figure deja dans `customer`, la hacher ici
 * n'apporterait aucune protection et empecherait la purge par adresse en cas
 * de demande d'effacement.
 */
export const quoteLinkRequest = pgTable(
  'quote_link_request',
  {
    id: uuid('id').primaryKey().defaultRandom(),
    email: text('email').notNull(),
    requestedAt: timestamp('requested_at', { withTimezone: true }).notNull().defaultNow(),
  },
  (table) => [index('quote_link_request_email_idx').on(table.email, table.requestedAt)],
)
```

- [ ] **Step 3 : Exporter**

Ajouter `export * from './quote-link-request'` dans `src/db/schema/index.ts`, en respectant l'ordre alphabétique des lignes existantes.

- [ ] **Step 4 : Générer et appliquer la migration**

Run : `pnpm db:generate`
Expected : un nouveau fichier sous `supabase/migrations/`.

Run : `pnpm db:reset`
Expected : toutes les migrations s'appliquent, la nouvelle comprise.

- [ ] **Step 5 : Commit**

```bash
git add src/db/schema supabase/migrations docs/superpowers/specs/2026-08-08-landing-design.md
git commit -m "feat: table de garde du renvoi de lien, et correction RGPD de la spec"
```

---

## Task 10 : La décision de limitation, en fonction pure

**Files:**
- Create: `src/domain/rate-limit.ts`
- Create: `tests/domain/rate-limit.test.ts`

- [ ] **Step 1 : Le test**

```ts
// tests/domain/rate-limit.test.ts
import { describe, expect, it } from 'vitest'
import { isRateLimited } from '@/domain/rate-limit'

const HOUR = 60 * 60 * 1000
const NOW = new Date('2026-08-08T12:00:00Z')

describe('isRateLimited', () => {
  it('laisse passer quand rien n’a ete demande', () => {
    expect(isRateLimited([], NOW, HOUR, 3)).toBe(false)
  })

  it('laisse passer en dessous du plafond', () => {
    const recent = [new Date(NOW.getTime() - 60_000), new Date(NOW.getTime() - 120_000)]
    expect(isRateLimited(recent, NOW, HOUR, 3)).toBe(false)
  })

  it('bloque au plafond', () => {
    const recent = [1, 2, 3].map((m) => new Date(NOW.getTime() - m * 60_000))
    expect(isRateLimited(recent, NOW, HOUR, 3)).toBe(true)
  })

  it('ignore ce qui est sorti de la fenetre', () => {
    const old = [1, 2, 3].map((h) => new Date(NOW.getTime() - h * HOUR - 1000))
    expect(isRateLimited(old, NOW, HOUR, 3)).toBe(false)
  })

  it('compte la limite exactement sur le bord de fenetre', () => {
    // Une demande vieille d'exactement une heure est HORS fenetre : sinon le
    // plafond se decale d'une milliseconde a chaque appel.
    const edge = [new Date(NOW.getTime() - HOUR)]
    expect(isRateLimited(edge, NOW, HOUR, 1)).toBe(false)
  })
})
```

- [ ] **Step 2 : Lancer, voir échouer**

Run : `pnpm exec vitest run tests/domain/rate-limit.test.ts`
Expected : ÉCHEC — module introuvable.

- [ ] **Step 3 : Implémenter**

```ts
// src/domain/rate-limit.ts

/**
 * Decide si une action doit etre refusee, sans rien connaitre de la base.
 *
 * Fonction pure prenant l'instant en parametre : une limitation qui lit
 * l'horloge implicitement est intestable, et le meme choix a ete fait pour
 * `paymentStatus`.
 *
 * La borne est **stricte** : une demande vieille d'exactement la fenetre est
 * dehors. Sinon le plafond glisse d'une milliseconde a chaque appel.
 */
export function isRateLimited(
  timestamps: Date[],
  now: Date,
  windowMs: number,
  max: number,
): boolean {
  const floor = now.getTime() - windowMs
  const recent = timestamps.filter((at) => at.getTime() > floor)
  return recent.length >= max
}
```

- [ ] **Step 4 : Lancer**

Run : `pnpm exec vitest run tests/domain/rate-limit.test.ts`
Expected : PASS, 5 cas.

- [ ] **Step 5 : Commit**

```bash
git add src/domain/rate-limit.ts tests/domain/rate-limit.test.ts
git commit -m "feat: la decision de limitation de debit, en fonction pure"
```

---

## Task 11 : Le service et l'action de renvoi

**Files:**
- Create: `src/services/quote-link.ts`
- Modify: `src/app/actions/public.ts`

- [ ] **Step 1 : Le service**

```ts
// src/services/quote-link.ts
import { and, eq, gt, lt, ne } from 'drizzle-orm'
import { db } from '@/db/client'
import { customer, project, quote, quoteLinkRequest } from '@/db/schema'
import { format } from '@/domain/money'
import { isRateLimited } from '@/domain/rate-limit'
import { sendQuoteLink } from './email'

const WINDOW_MS = 60 * 60 * 1000
const MAX_PER_WINDOW = 3
const RETENTION_MS = 24 * 60 * 60 * 1000

/**
 * Renvoie a une adresse les liens des devis qui lui ont ete adresses.
 *
 * Ne renvoie **jamais** ce qu'elle a trouve : l'appelant ne doit rien pouvoir
 * deduire, sans quoi le formulaire devient un test d'existence d'adresse.
 */
export async function resendQuoteLinks(email: string, now: Date): Promise<void> {
  const address = email.trim().toLowerCase()

  // Purge a l'ecriture plutot que par tache planifiee : une tache qu'on oublie
  // de surveiller laisse grossir une table de donnees personnelles.
  await db
    .delete(quoteLinkRequest)
    .where(lt(quoteLinkRequest.requestedAt, new Date(now.getTime() - RETENTION_MS)))

  const previous = await db
    .select({ requestedAt: quoteLinkRequest.requestedAt })
    .from(quoteLinkRequest)
    .where(
      and(
        eq(quoteLinkRequest.email, address),
        gt(quoteLinkRequest.requestedAt, new Date(now.getTime() - WINDOW_MS)),
      ),
    )

  await db.insert(quoteLinkRequest).values({ email: address, requestedAt: now })

  if (isRateLimited(previous.map((r) => r.requestedAt), now, WINDOW_MS, MAX_PER_WINDOW)) {
    return
  }

  // Jamais un brouillon : il n'a pas d'adresse publique.
  const found = await db
    .select({
      number: quote.number,
      publicToken: quote.publicToken,
      totalInclTax: quote.totalInclTax,
      customerName: customer.name,
      companyName: project.label,
    })
    .from(quote)
    .innerJoin(project, eq(quote.projectId, project.id))
    .innerJoin(customer, eq(project.customerId, customer.id))
    .where(and(eq(customer.email, address), ne(quote.status, 'draft')))

  const base = process.env.APP_URL ?? ''

  for (const row of found) {
    await sendQuoteLink({
      to: address,
      customerName: row.customerName,
      companyName: row.companyName,
      quoteNumber: row.number ?? '',
      totalInclTax: format(row.totalInclTax),
      link: `${base}/d/${row.publicToken}`,
    })
  }
}
```

**Avant d'écrire, vérifier deux choses** : que `project` porte bien un champ utilisable comme `companyName` — sinon joindre `company` et prendre `company.legalName` —, et que `quote.number` est bien nullable.

- [ ] **Step 2 : L'action**

Ajouter à `src/app/actions/public.ts` :

```ts
import { resendQuoteLinks } from '@/services/quote-link'

export interface QuoteLinkState {
  sent?: boolean
  error?: string
}

/** Le message de succes, unique et neutre. */
const NEUTRAL = 'Si un devis a été envoyé à cette adresse, vous allez le recevoir.'

/**
 * Renvoie le lien d'un devis a l'adresse a laquelle il a ete adresse.
 *
 * **La reponse est toujours la meme**, qu'un devis existe ou non, que la
 * limitation ait joue ou non. Sans cela le formulaire devient un test
 * d'existence d'adresse. Le lien n'est jamais affiche, uniquement envoye.
 */
export async function requestQuoteLink(
  _state: QuoteLinkState,
  form: FormData,
): Promise<QuoteLinkState> {
  const email = String(form.get('email') ?? '').trim()

  if (!email.includes('@')) {
    return { error: 'Cette adresse ne semble pas valide.' }
  }

  await resendQuoteLinks(email, new Date())
  return { sent: true }
}

export { NEUTRAL as QUOTE_LINK_CONFIRMATION }
```

- [ ] **Step 3 : Vérifier**

Run : `pnpm exec tsc --noEmit && pnpm exec vitest run`
Expected : aucune erreur, tous les tests passent.

- [ ] **Step 4 : Commit**

```bash
git add src/services/quote-link.ts src/app/actions/public.ts
git commit -m "feat: le renvoi de lien de devis, borne et a reponse neutre"
```

---

## Task 12 : `QuoteLinkForm`

**Files:**
- Create: `src/ui/organisms/quote-link-form.tsx`

- [ ] **Step 1 : Écrire le composant**

```tsx
// src/ui/organisms/quote-link-form.tsx
'use client'

import { useActionState } from 'react'
import {
  QUOTE_LINK_CONFIRMATION,
  requestQuoteLink,
  type QuoteLinkState,
} from '@/app/actions/public'
import { Button } from '@/ui/atoms/button'
import { Icon } from '@/ui/atoms/icon'
import { Input } from '@/ui/atoms/input'
import { Text } from '@/ui/atoms/text'
import { Field } from '@/ui/molecules/field'

const initialState: QuoteLinkState = {}

/**
 * Action **secondaire** de la page demandeur : bouton contour, jamais en
 * terre cuite. La verification reste l'action principale.
 */
export function QuoteLinkForm() {
  const [state, action, pending] = useActionState(requestQuoteLink, initialState)

  if (state.sent) {
    return (
      <div
        role="status"
        className="flex max-w-md items-center gap-3 rounded-card border border-verified bg-verified-bg px-5 py-4 text-verified"
      >
        <Icon name="check" />
        <Text as="span">{QUOTE_LINK_CONFIRMATION}</Text>
      </div>
    )
  }

  return (
    <form action={action} className="flex max-w-md flex-wrap items-end gap-3">
      <div className="min-w-56 flex-1">
        <Field label="Votre adresse e-mail" error={state.error} required>
          {(p) => <Input {...p} name="email" type="email" autoComplete="email" />}
        </Field>
      </div>
      <Button type="submit" tone="secondary" size="lg" pending={pending}>
        Recevoir le lien
      </Button>
    </form>
  )
}
```

- [ ] **Step 2 : Vérifier et committer**

Run : `pnpm exec tsc --noEmit && pnpm check:ds`

```bash
git add src/ui/organisms/quote-link-form.tsx
git commit -m "feat: QuoteLinkForm"
```

---

## Task 13 : La page demandeur

**Files:**
- Create: `src/app/verifier/page.tsx`
- Create: `src/app/verifier/opengraph-image.tsx`
- Create: `src/app/_landing/verifier/hero.tsx`
- Create: `src/app/_landing/verifier/trap.tsx`
- Create: `src/app/_landing/verifier/retrieve.tsx`
- Create: `src/app/_landing/verifier/notebook.tsx`

- [ ] **Step 1 : Les quatre sections**

Même motif que la page pro. Textes tirés du **§11 de la spec landing**. Points à respecter :

- `hero.tsx` — un `Reveal` avec `RevealTick`, titre « Votre artisan est-il assuré pour ce qu'il va faire ? », puis `<SiretLookup tone="conversion" label="SIRET de l'entreprise" cta="Vérifier" hint="Gratuit, sans compte. Le SIRET figure sur son devis." />`. **C'est le seul `tone="conversion"` du produit avec la signature de devis.**
- `trap.tsx` — `SectionHeader` étiquette « Le piège », puis trois `Card` en `Stagger`, fond alterné `bg-card border-y border-rule`.
- `retrieve.tsx` — `SectionHeader` étiquette « Vous attendiez un devis ? », puis `QuoteLinkForm`.
- `notebook.tsx` — `SectionHeader` étiquette « Bientôt » — **seule occurrence tolérée du mot dans tout le produit, parce qu'elle porte une condition explicite et non une date** — puis la mention « Aucune inscription pour l'instant. Nous ne collectons pas votre adresse. » en `Text size="sm" tone="muted"`.

- [ ] **Step 2 : La composition**

```tsx
// src/app/verifier/page.tsx
import type { Metadata } from 'next'
import { LandingShell } from '@/ui/shells/landing-shell'
import { Hero } from '../_landing/verifier/hero'
import { Notebook } from '../_landing/verifier/notebook'
import { Retrieve } from '../_landing/verifier/retrieve'
import { Trap } from '../_landing/verifier/trap'

export const metadata: Metadata = {
  title: "Vérifier l'assurance d'un artisan — D'équerre",
  description:
    'Une assurance décennale ne couvre que les activités qu’elle nomme. Entrez le SIRET de l’entreprise : nous affichons ce qui est couvert, et ce qui ne l’est pas.',
  alternates: { canonical: '/verifier' },
}

export default function Verifier() {
  return (
    <LandingShell audience="demandeur">
      <Hero />
      <Trap />
      <Retrieve />
      <Notebook />
    </LandingShell>
  )
}
```

- [ ] **Step 3 : L'image de partage**

Copier `src/app/opengraph-image.tsx` vers `src/app/verifier/opengraph-image.tsx` et n'en changer que deux choses : `alt`, et le titre rendu, qui devient « Votre artisan est-il assuré pour ce qu'il va faire ? » sur trois lignes, en `fontSize: 62`.

- [ ] **Step 4 : Vérifier**

Run : `pnpm exec tsc --noEmit && pnpm lint && pnpm check:ds && pnpm check:size && pnpm build`

- [ ] **Step 5 : Vérifier à l'œil et en usage**

Run : `pnpm dev`, ouvrir `http://localhost:3000/verifier`
Attendu : le champ SIRET est en terre cuite ; une entreprise sans page publique et une entreprise inexistante donnent **le même message** ; le renvoi de lien affiche la confirmation neutre **même pour une adresse inconnue**.

- [ ] **Step 6 : Commit**

```bash
git add src/app/verifier src/app/_landing/verifier
git commit -m "feat: la page demandeur"
```

---

## Task 14 : Vérification finale

- [ ] **Step 1 : La chaîne complète**

Run : `pnpm validate`
Expected : `check:env`, `check:size`, `check:ds`, `check:isolation`, `build` et `vitest` tous verts.

- [ ] **Step 2 : Les parcours existants**

Run : `pnpm test:e2e`
Expected : les trois parcours restent verts. Le seul risque est `/` : le parcours de devis démarre par `page.goto('/connexion')`, mais **vérifier qu'aucun test ne passe par l'accueil** avec `grep -n "goto('/')" tests/e2e/*.spec.ts`.

- [ ] **Step 3 : Les deux thèmes et les deux largeurs**

Ouvrir `/` et `/verifier` à 375 px et en 1440 px. Les deux pages forcent le mode clair par `LandingShell` : vérifier que la bascule de thème du système ne les altère pas, et qu'aucune ne défile horizontalement.

- [ ] **Step 4 : Mouvement réduit**

Activer « Réduire les animations » dans les préférences du système, recharger les deux pages.
Expected : tout le contenu est visible immédiatement, aucun mouvement.

- [ ] **Step 5 : Sans JavaScript**

Désactiver JavaScript dans le navigateur, recharger `/`.
Expected : **la page reste entièrement lisible**. C'est le rôle de la classe `dq-motion` : sans elle, aucun état initial masqué n'est appliqué.

- [ ] **Step 6 : Commit et push**

```bash
git add -A
git commit -m "docs: plan landing execute"
git push
```

---

## Auto-revue

**Couverture de la spec.** §1 architecture → Tasks 5, 7, 13. §2 ce qu'on ne promet pas → Task 7 Step 3, section `next.tsx`. §3 modèle économique → Task 7 Step 3, `pricing.tsx` et `principles.tsx`. §4 les sept sections → Task 7. §5 les quatre sections → Task 13. §6.1 vérification → Task 6. §6.2 renvoi de lien → Tasks 9 à 12. §7.1 inventaire → Task 1. §7.2 dossier privé → Tasks 7 et 13. §8 régime de mouvement → Tasks 2 et 3. §9 référencement → Tasks 7, 8 et 13. §10 point 1, limitation de débit → Tasks 9 à 11.

**Correction apportée à la spec en cours de route.** Le §6.2 affirmait « aucun traitement nouveau ». C'est faux dès qu'on compte les demandes : la Task 9 Step 1 corrige la spec avant de créer la table.

**Cohérence des types.** `LookupState` et `QuoteLinkState` sont définis en Task 6 et Task 11, dans le même fichier `src/app/actions/public.ts`, et consommés en Tasks 6 et 12. `parseSiretInput` renvoie `{ siren } | { error }` — le discriminant testé est `'error' in parsed`, jamais une comparaison à `null`. `isRateLimited(timestamps, now, windowMs, max)` garde le même ordre d'arguments entre sa définition (Task 10) et son appel (Task 11). `SiretLookup` accepte `tone`, `label`, `cta`, `hint` en Task 6 et reçoit exactement ces quatre props en Tasks 7 et 13.

**Trois points à surveiller à l'exécution**, signalés dans les tâches concernées : la forme réelle de `normalizeSiret` (Task 6 Step 3), l'existence d'un `companyName` exploitable sur `project` (Task 11 Step 1), et le fait qu'un `Font.register` d'`ImageResponse` qui échoue ne lève pas — il faut comparer l'image à l'œil (Task 8 Step 3).

**Ce que ce plan ne fait pas.** La rédaction définitive de la section « la suite » reste ouverte (§10 point 3 de la spec) : la Task 7 en écrit une première version et demande sa relecture. Aucun prix n'apparaît nulle part.
